# 화면별 상세 기능 명세서

Flutter 프로젝트의 모든 화면을 Next.js/React로 구현하기 위한 상세 명세서입니다.

---

## 📋 목차

### 인증 관련 (2개)
1. [LoginScreen - 로그인](#1-loginscreen---로그인)
2. [SignupScreen - 회원가입](#2-signupscreen---회원가입)

### 공통 화면 (2개)
3. [HomeScreen - 홈화면](#3-homescreen---홈화면)
4. [ProductListScreen - 상품 목록](#4-productlistscreen---상품-목록)

### 상품 관련 (2개)
5. [ProductDetailScreen - 상품 상세](#5-productdetailscreen---상품-상세)
6. [ProductFormScreen - 상품 등록/수정](#6-productformscreen---상품-등록수정)

### 문의 관련 (1개)
7. [InquiryListScreen - 문의 목록](#7-inquirylistscreen---문의-목록)

### 사업자 인증 (1개)
8. [BusinessVerificationScreen - 사업자 인증 신청](#8-businessverificationscreen---사업자-인증-신청)

### 도매상 대시보드 (2개)
9. [WholesalerDashboardScreen - 도매상 대시보드](#9-wholesalerdashboardscreen---도매상-대시보드)
10. [MyOffersScreen - 내 제안 목록](#10-myoffersscreen---내-제안-목록)

### 관리자 (1개)
11. [AdminDashboardScreen - 관리자 대시보드](#11-admindashboardscreen---관리자-대시보드)

### 매입 요청 시스템 (4개)
12. [CreateSellRequestScreen - 매입 요청 등록](#12-createsellrequestscreen---매입-요청-등록)
13. [MySellRequestsScreen - 내 매입 요청 목록](#13-mysellrequestsscreen---내-매입-요청-목록)
14. [SellRequestListScreen - 매입 요청 목록 (도매상용)](#14-sellrequestlistscreen---매입-요청-목록-도매상용)
15. [SellRequestDetailScreen - 매입 요청 상세](#15-sellrequestdetailscreen---매입-요청-상세)

---

## 1. LoginScreen - 로그인

**경로**: `/login`

**파일**: `lib/screens/login_screen.dart`

### 주요 기능
- 이메일/비밀번호 로그인
- 비밀번호 재설정 이메일 전송
- 회원가입 화면으로 이동
- 로그인 성공 시 HomeScreen으로 리다이렉트

### UI 컴포넌트
- 이메일 입력 필드 (TextField)
- 비밀번호 입력 필드 (TextField, obscureText)
- 로그인 버튼 (ElevatedButton)
- 비밀번호 찾기 링크 (TextButton)
- 회원가입 링크 (TextButton)
- 로딩 인디케이터

### 상태 관리
- `StatefulWidget` 사용
- Provider를 통한 AuthProvider 접근

### Firebase 상호작용
- **AuthService.signIn()** 호출
- 컬렉션: `users` (읽기)

### 폼 필드
```typescript
{
  email: string;        // 이메일 (필수, 이메일 형식 검증)
  password: string;     // 비밀번호 (필수, 최소 6자)
}
```

### 에러 처리
- 이메일 형식 검증
- Firebase Auth 에러 메시지 한글 변환
- SnackBar로 에러 표시

### Next.js 구현 시 주의사항
- Client Component (`'use client'`)
- React Hook Form + Zod 사용 권장
- Firebase Auth `signInWithEmailAndPassword()` 사용
- 로그인 후 리다이렉트: `useRouter().push('/')`

---

## 2. SignupScreen - 회원가입

**경로**: `/signup`

**파일**: `lib/screens/signup_screen.dart`

### 주요 기능
- 신규 사용자 등록
- 사용자 타입 선택 (일반 사용자 / 도매상)
- 이메일 중복 확인
- 비밀번호 강도 검증

### UI 컴포넌트
- 이름 입력 필드
- 이메일 입력 필드
- 비밀번호 입력 필드
- 비밀번호 확인 입력 필드
- 사용자 타입 선택 (라디오 버튼 또는 드롭다운)
- 회원가입 버튼
- 로그인 화면 링크

### 상태 관리
- `StatefulWidget`
- 폼 유효성 검증 상태

### Firebase 상호작용
- **AuthService.signUp()** 호출
- 컬렉션: `users` (생성)

### 폼 필드
```typescript
{
  name: string;              // 이름 (필수)
  email: string;             // 이메일 (필수, 유효성 검증)
  password: string;          // 비밀번호 (필수, 최소 6자)
  confirmPassword: string;   // 비밀번호 확인 (password와 일치)
  userType: 'normal' | 'wholesaler'; // 사용자 타입
}
```

### 유효성 검증
- 이메일 형식
- 비밀번호 최소 길이 (6자)
- 비밀번호 일치 확인
- 모든 필드 필수 입력

### Next.js 구현 시 주의사항
- Client Component
- React Hook Form + Zod schema validation
- Firebase Auth `createUserWithEmailAndPassword()`
- Firestore에 사용자 문서 추가: `setDoc(doc(db, 'users', uid), ...)`
- 회원가입 성공 시 자동 로그인 및 리다이렉트

---

## 3. HomeScreen - 홈화면

**경로**: `/`

**파일**: `lib/screens/home_screen.dart`

### 주요 기능
- 사용자 타입에 따른 화면 분기
  - 일반 사용자 → ProductListScreen
  - 도매상 (승인됨) → WholesalerDashboardScreen
  - 도매상 (미승인) → 승인 대기 안내 화면
  - 관리자 → AdminDashboardScreen
- 로그아웃 기능
- 상단 네비게이션 바

### UI 컴포넌트
- AppBar (상단 네비게이션)
- Drawer (사이드 메뉴)
- 사용자 정보 표시
- 로그아웃 버튼

### 상태 관리
- Provider (`context.watch<AuthProvider>()`)
- 현재 사용자 정보 구독

### Firebase 상호작용
- **AuthProvider.currentUser** 접근
- 컬렉션: `users` (읽기 - 실시간 구독)

### 화면 전환
```
일반 사용자 → ProductListScreen
도매상 (승인) → WholesalerDashboardScreen
도매상 (미승인) → 승인 대기 안내
관리자 → AdminDashboardScreen
```

### Next.js 구현 시 주의사항
- Server Component 가능
- `getServerSession()` 또는 Client에서 `useAuth()` 훅 사용
- Layout 컴포넌트로 구현 추천
- 사용자 타입별 라우팅:
  ```typescript
  if (user.userType === 'admin') return <AdminDashboard />
  if (user.userType === 'wholesaler' && user.verificationStatus === 'approved')
    return <WholesalerDashboard />
  return <ProductList />
  ```

---

## 4. ProductListScreen - 상품 목록

**경로**: `/products`

**파일**: `lib/screens/product_list_screen.dart`

### 주요 기능
- 모든 판매 중인 상품 그리드 뷰 표시
- 실시간 상품 검색 (제목 기반)
- 카테고리별 필터링
- 상품 상세 페이지로 이동
- FloatingActionButton: 매입 요청 등록 (일반 사용자만)

### UI 컴포넌트
- SearchBar (검색 입력 필드)
- GridView (상품 카드 그리드)
- ProductCard (이미지, 제목, 가격, 판매자)
- CategoryFilter (카테고리 선택 칩)
- FloatingActionButton (매입 요청)

### 상태 관리
- `StatefulWidget`
- StreamBuilder (실시간 상품 목록)
- 검색어 상태 관리

### Firebase 상호작용
- **ProductService.getAllProducts()** (Stream)
- 컬렉션: `products` (읽기 - 실시간)
- 쿼리: `where('isAvailable', isEqualTo: true)`

### 검색 기능
- 클라이언트 사이드 필터링 (제목 포함 여부)
- 대소문자 구분 없음

### 화면 전환
- 상품 카드 클릭 → ProductDetailScreen
- FAB 클릭 → CreateSellRequestScreen

### Next.js 구현 시 주의사항
- Client Component (실시간 데이터)
- `onSnapshot()` 사용 또는 SWR/React Query
- 검색 디바운싱 구현 (`useDebouncedValue`)
- 무한 스크롤 또는 페이지네이션 추가 권장
- 이미지 최적화: Next.js `<Image>` 사용

```typescript
// 예시 코드
const ProductList = () => {
  const [products, setProducts] = useState<Product[]>([]);
  const [search, setSearch] = useState('');

  useEffect(() => {
    const q = query(
      collection(db, 'products'),
      where('isAvailable', '==', true),
      orderBy('createdAt', 'desc')
    );

    const unsubscribe = onSnapshot(q, (snapshot) => {
      const data = snapshot.docs.map(firestoreToProduct).filter(Boolean);
      setProducts(data);
    });

    return () => unsubscribe();
  }, []);

  const filteredProducts = products.filter(p =>
    p.title.toLowerCase().includes(search.toLowerCase())
  );

  return <div className="grid grid-cols-3 gap-4">...</div>;
};
```

---

## 5. ProductDetailScreen - 상품 상세

**경로**: `/products/[id]`

**파일**: `lib/screens/product_detail_screen.dart`

### 주요 기능
- 상품 상세 정보 표시
- 이미지 갤러리 (여러 이미지 슬라이드)
- 판매자 정보 표시
- 문의하기 버튼 (일반 사용자만)
- 수정/삭제 버튼 (본인 상품만)

### UI 컴포넌트
- 이미지 캐러셀 (PageView)
- 상품 정보 섹션 (제목, 가격, 설명, 카테고리, 재고)
- 판매자 정보 카드
- 문의하기 다이얼로그
- 수정/삭제 버튼

### 상태 관리
- `StatefulWidget`
- FutureBuilder (상품 정보 로드)

### Firebase 상호작용
- **ProductService.getProduct(productId)** (읽기)
- **InquiryService.createInquiry()** (문의 작성)
- **ProductService.deleteProduct()** (삭제)
- 컬렉션: `products`, `inquiries`

### 문의 작성 폼
```typescript
{
  question: string; // 문의 내용 (필수)
}
```

### 권한 체크
- 수정/삭제: `product.sellerId === currentUser.uid`
- 문의하기: 일반 사용자만 (도매상은 불가)

### 화면 전환
- 수정 버튼 → ProductFormScreen (편집 모드)
- 뒤로가기 → ProductListScreen

### Next.js 구현 시 주의사항
- Dynamic Route: `app/products/[id]/page.tsx`
- Server Component로 초기 데이터 로드 가능:
  ```typescript
  export default async function ProductDetailPage({ params }: { params: { id: string } }) {
    const product = await getProduct(params.id);
    return <ProductDetailClient product={product} />;
  }
  ```
- 이미지 갤러리: `swiper` 또는 `react-responsive-carousel`
- 문의 모달: Radix UI Dialog 또는 Headless UI

---

## 6. ProductFormScreen - 상품 등록/수정

**경로**: `/products/new`, `/products/[id]/edit`

**파일**: `lib/screens/product_form_screen.dart`

### 주요 기능
- 새 상품 등록 (도매상만)
- 기존 상품 수정 (본인 상품만)
- 다중 이미지 업로드 (최대 5장)
- 이미지 미리보기
- 폼 유효성 검증

### UI 컴포넌트
- 이미지 선택 버튼
- 이미지 미리보기 그리드
- 제목 입력 필드
- 설명 입력 필드 (멀티라인)
- 가격 입력 필드 (숫자만)
- 수량 입력 필드
- 카테고리 드롭다운
- 저장 버튼

### 상태 관리
- `StatefulWidget`
- 이미지 파일 리스트 상태
- 폼 필드 상태

### Firebase 상호작용
- **ProductService.addProduct()** (등록)
- **ProductService.updateProduct()** (수정)
- **Firebase Storage**: 이미지 업로드
- 컬렉션: `products`
- 스토리지 경로: `products/{sellerId}/{fileName}`

### 폼 필드
```typescript
{
  title: string;          // 제품명 (필수)
  description: string;    // 설명 (필수)
  price: number;          // 가격 (필수, 양수)
  quantity: number;       // 수량 (필수, 양수)
  category: string;       // 카테고리 (필수)
  imageFiles: File[];     // 이미지 파일들 (최대 5개)
}
```

### 유효성 검증
- 모든 필드 필수
- 가격/수량 양수 확인
- 이미지 최대 5개 제한
- 이미지 파일 크기 제한 (예: 5MB)

### Next.js 구현 시 주의사항
- Client Component
- `react-hook-form` + Zod
- 이미지 업로드: `react-dropzone` 또는 `<input type="file" multiple>`
- Firebase Storage 업로드:
  ```typescript
  const uploadImages = async (files: File[]) => {
    const urls = await Promise.all(
      files.map(async (file, i) => {
        const fileName = `${Date.now()}_${i}.jpg`;
        const storageRef = ref(storage, `products/${userId}/${fileName}`);
        await uploadBytes(storageRef, file);
        return getDownloadURL(storageRef);
      })
    );
    return urls;
  };
  ```
- 로딩 상태 표시 (업로드 진행률)

---

## 7. InquiryListScreen - 문의 목록

**경로**: `/inquiries`

**파일**: `lib/screens/inquiry_list_screen.dart`

### 주요 기능
- 내가 작성한 문의 목록 (일반 사용자)
- 받은 문의 목록 (도매상)
- 답변 대기 / 답변 완료 필터
- 문의 상세 보기 (확장 가능한 리스트)
- 답변 작성 (도매상만)

### UI 컴포넌트
- 탭 바 (내 문의 / 받은 문의)
- ExpansionTile 리스트
- 문의 내용 표시
- 답변 입력 필드 (도매상용)
- 답변 제출 버튼

### 상태 관리
- `StatefulWidget`
- StreamBuilder (문의 목록)

### Firebase 상호작용
- **InquiryService.getMyInquiries(userId)** (일반 사용자)
- **InquiryService.getInquiriesBySeller(sellerId)** (도매상)
- **InquiryService.answerInquiry()** (답변 작성)
- 컬렉션: `inquiries`

### 답변 작성 폼
```typescript
{
  answer: string; // 답변 내용 (필수)
}
```

### Next.js 구현 시 주의사항
- Client Component
- Accordion UI (Radix UI Accordion 또는 Headless UI Disclosure)
- 실시간 업데이트: `onSnapshot()`
- 사용자 타입별 쿼리 분기:
  ```typescript
  const q = userType === 'wholesaler'
    ? query(collection(db, 'inquiries'), where('sellerId', '==', uid))
    : query(collection(db, 'inquiries'), where('customerId', '==', uid));
  ```

---

## 8. BusinessVerificationScreen - 사업자 인증 신청

**경로**: `/business-verification`

**파일**: `lib/screens/business_verification_screen.dart`

### 주요 기능
- 사업자등록증 이미지 업로드
- 사업자 정보 입력
- 인증 신청 제출
- 승인 상태 확인

### UI 컴포넌트
- 이미지 선택 버튼
- 이미지 미리보기
- 사업자등록번호 입력 (10자리)
- 상호명 입력
- 대표자명 입력
- 신청 버튼
- 상태 표시 (대기/승인/거부)

### 상태 관리
- `StatefulWidget`
- 이미지 파일 상태
- 폼 필드 상태

### Firebase 상호작용
- **BusinessVerificationService.submitVerification()**
- **Firebase Storage**: 사업자등록증 업로드
- 컬렉션: `business_verifications`
- 스토리지 경로: `business_documents/{userId}/{fileName}`

### 폼 필드
```typescript
{
  businessNumber: string;      // 사업자등록번호 (10자리, 숫자만)
  companyName: string;         // 상호명 (필수)
  representativeName: string;  // 대표자명 (필수)
  documentImage: File;         // 사업자등록증 이미지 (필수)
}
```

### 유효성 검증
- 사업자등록번호 10자리 검증
- 모든 필드 필수
- 이미지 파일 형식 및 크기 검증

### Next.js 구현 시 주의사항
- Client Component
- 사업자등록번호 마스킹: `000-00-00000`
- 파일 업로드: Storage에 먼저 업로드 후 URL 획득
- 신청 후 users 컬렉션의 verificationStatus 업데이트 고려

---

## 9. WholesalerDashboardScreen - 도매상 대시보드

**경로**: `/wholesaler/dashboard`

**파일**: `lib/screens/wholesaler_dashboard_screen.dart`

### 주요 기능
- 등록한 상품 통계 (총 상품 수, 총 재고 가치)
- 받은 문의 알림 배지
- 상품 목록 바로가기
- 문의 목록 바로가기
- 매입 요청 목록 바로가기
- 상품 등록 버튼

### UI 컴포넌트
- 통계 카드 (상품 수, 재고 가치, 문의 수)
- 빠른 링크 버튼
- 최근 등록 상품 리스트

### 상태 관리
- `StatefulWidget`
- StreamBuilder (실시간 통계)

### Firebase 상호작용
- **ProductService.getProductsBySeller(uid)** (Stream)
- **InquiryService.getInquiriesBySeller(uid)** (Stream)
- 컬렉션: `products`, `inquiries`

### 통계 계산
```typescript
totalProducts = products.length;
totalInventoryValue = products.reduce((sum, p) => sum + (p.price * p.quantity), 0);
unansweredInquiries = inquiries.filter(i => i.status === 'pending').length;
```

### 화면 전환
- 상품 관리 → ProductListScreen (본인 상품만 필터)
- 문의 관리 → InquiryListScreen
- 매입 요청 → SellRequestListScreen
- 상품 등록 → ProductFormScreen

### Next.js 구현 시 주의사항
- Client Component
- 여러 컬렉션 동시 구독 필요
- 통계는 클라이언트에서 계산 또는 Server Action 사용
- 차트 라이브러리: `recharts` 또는 `chart.js`

---

## 10. MyOffersScreen - 내 제안 목록

**경로**: `/my-offers`

**파일**: `lib/screens/my_offers_screen.dart`

### 주요 기능
- 도매상이 제출한 매입 제안 목록
- 제안 상태 (대기 / 선택됨)
- 매입 요청 상세로 이동

### UI 컴포넌트
- 제안 카드 리스트
- 상태 배지
- 제안 금액 표시

### 상태 관리
- StreamBuilder

### Firebase 상호작용
- **SellRequestService.getMyOffers(wholesalerId)**
- 컬렉션: `purchase_offers`

### Next.js 구현 시 주의사항
- 제안과 연결된 매입 요청 정보도 함께 표시 필요
- Join 쿼리 대신 클라이언트 사이드 데이터 결합

---

## 11. AdminDashboardScreen - 관리자 대시보드

**경로**: `/admin/dashboard`

**파일**: `lib/screens/admin_dashboard_screen.dart`

### 주요 기능
- 사업자 인증 승인 대기 목록
- 사업자 인증 승인/거부 처리
- 전체 인증 내역 조회
- 거부 사유 입력

### UI 컴포넌트
- 승인 대기 목록 (카드 형식)
- 사업자등록증 이미지 뷰어
- 승인/거부 버튼
- 거부 사유 입력 다이얼로그

### 상태 관리
- StreamBuilder (실시간 인증 목록)

### Firebase 상호작용
- **BusinessVerificationService.getPendingVerifications()** (Stream)
- **BusinessVerificationService.approveVerification()**
- **BusinessVerificationService.rejectVerification()**
- 컬렉션: `business_verifications`, `users`

### 승인/거부 처리
- 승인 시: `business_verifications` 상태 변경 + `users.verificationStatus` 변경
- 거부 시: 거부 사유 입력 필수

### Next.js 구현 시 주의사항
- 관리자 권한 체크 필수 (Middleware 또는 Server Component)
- 트랜잭션 사용: 두 컬렉션 동시 업데이트
- 이미지 모달: 큰 이미지로 확대 보기

---

## 12. CreateSellRequestScreen - 매입 요청 등록

**경로**: `/sell-requests/new`

**파일**: `lib/screens/create_sell_request_screen.dart`

### 주요 기능
- 중고 컴퓨터 매입 요청 등록
- 다중 이미지 업로드 (Web/모바일 호환)
- 희망 가격 입력 (선택)
- 상세 설명 입력

### UI 컴포넌트
- 이미지 선택 버튼
- 이미지 미리보기
- 제목 입력
- 설명 입력 (멀티라인)
- 희망 가격 입력 (선택)
- 등록 버튼

### 상태 관리
- StatefulWidget
- 이미지 파일 리스트

### Firebase 상호작용
- **SellRequestService.createSellRequest()**
- **Firebase Storage**: 이미지 업로드
- 컬렉션: `sell_requests`
- 스토리지 경로: `sell_requests/{fileName}`

### 폼 필드
```typescript
{
  title: string;           // 제목 (필수)
  description: string;     // 설명 (필수)
  desiredPrice?: string;   // 희망 가격 (선택)
  imageFiles: File[];      // 이미지 (최대 10개)
}
```

### Next.js 구현 시 주의사항
- Web에서 이미지 업로드 처리 주의
- Flutter의 `XFile` 대신 `File` API 사용
- 업로드 진행률 표시

---

## 13. MySellRequestsScreen - 내 매입 요청 목록

**경로**: `/my-sell-requests`

**파일**: `lib/screens/my_sell_requests_screen.dart`

### 주요 기능
- 내가 올린 매입 요청 목록
- 받은 제안 개수 실시간 표시
- 상태별 구분 (진행중/거래완료/취소)
- 매입 요청 상세로 이동

### UI 컴포넌트
- 요청 카드 리스트
- 상태 배지
- 제안 개수 표시

### 상태 관리
- StreamBuilder (두 개의 스트림: sell_requests + purchase_offers 카운트)

### Firebase 상호작용
- **SellRequestService.getMySellRequests(userId)**
- **SellRequestService.getOfferCount(requestId)** (각 요청별)
- 컬렉션: `sell_requests`, `purchase_offers`

### Next.js 구현 시 주의사항
- 제안 개수 계산을 위한 추가 쿼리 필요
- 실시간 업데이트 유지

---

## 14. SellRequestListScreen - 매입 요청 목록 (도매상용)

**경로**: `/sell-requests`

**파일**: `lib/screens/sell_request_list_screen.dart`

### 주요 기능
- 진행 중인 모든 매입 요청 조회
- 썸네일 이미지 표시
- 희망가 표시
- 매입 요청 상세로 이동

### UI 컴포넌트
- 매입 요청 카드 그리드
- 썸네일 이미지
- 제목, 설명 미리보기

### 상태 관리
- StreamBuilder

### Firebase 상호작용
- **SellRequestService.getAllSellRequests()** (Stream)
- 컬렉션: `sell_requests`
- 쿼리: `where('status', '==', 'open')`

### Next.js 구현 시 주의사항
- 도매상만 접근 가능 (권한 체크)
- 실시간 업데이트

---

## 15. SellRequestDetailScreen - 매입 요청 상세

**경로**: `/sell-requests/[id]`

**파일**: `lib/screens/sell_request_detail_screen.dart`

### 주요 기능

#### 일반 사용자 뷰:
- 받은 모든 제안 공개
- 가격 높은 순 정렬
- 도매상 선택 및 거래 확정

#### 도매상 뷰:
- 매입가 제시 (한 번만 가능)
- 블라인드 입찰 현황 확인
  - 총 입찰자 수 표시
  - 내 제안만 상세 확인 가능
  - 다른 도매상 제안은 비공개

### UI 컴포넌트
- 이미지 갤러리
- 매입 요청 정보
- 매입가 제시 폼 (도매상용)
- 제안 목록 (일반 사용자용)
- 거래 확정 버튼

### 상태 관리
- FutureBuilder + StreamBuilder

### Firebase 상호작용
- **SellRequestService.getSellRequest(requestId)**
- **SellRequestService.getOffers(requestId)**
- **SellRequestService.createOffer()** (도매상)
- **SellRequestService.selectWholesaler()** (일반 사용자)
- 컬렉션: `sell_requests`, `purchase_offers`

### 제안 생성 폼 (도매상)
```typescript
{
  offerPrice: number;   // 제시 매입가 (필수, 양수)
  message?: string;     // 도매상 메시지 (선택)
}
```

### 블라인드 로직
- 도매상은 자신의 제안만 상세 확인
- 다른 도매상 제안 존재 여부만 표시
- 일반 사용자는 모든 제안 전체 공개

### Firestore Rules
```javascript
// purchase_offers: 도매상은 읽기 가능하지만 앱에서 자신의 제안만 필터링
allow read: if isAuthenticated() && isApprovedWholesaler();

// 수정/삭제 불가
allow update: if false;
allow delete: if false;
```

### Next.js 구현 시 주의사항
- Dynamic Route
- 사용자 타입별 뷰 분기
- 블라인드 처리 로직 클라이언트에서 구현:
  ```typescript
  const myOffer = offers.find(o => o.wholesalerId === currentUser.uid);
  const otherOffersCount = offers.filter(o => o.wholesalerId !== currentUser.uid).length;

  // 일반 사용자는 모든 제안 표시
  const visibleOffers = currentUser.uid === sellRequest.sellerId
    ? offers
    : offers.filter(o => o.wholesalerId === currentUser.uid);
  ```

---

## 🎨 공통 UI/UX 가이드라인

### 색상 팔레트
```typescript
const colors = {
  primary: '#2563EB',      // 프리미엄 블루
  secondary: '#8B5CF6',    // 퍼플
  surface: '#FAFAFA',      // 배경
  background: '#F5F5F5',   // 전체 배경
  text: '#1F2937',         // 텍스트
  textSecondary: '#6B7280', // 보조 텍스트
};
```

### 타이포그래피
```css
headlineLarge: 32px, weight: 700
headlineMedium: 24px, weight: 600
titleLarge: 18px, weight: 600
bodyLarge: 16px, weight: 400
bodyMedium: 14px, weight: 400
```

### 버튼 스타일
- border-radius: 12px
- padding: 16px 24px
- elevation: 0 (flat design)

### 카드 스타일
- border-radius: 16px
- border: 1px solid #E5E7EB
- background: white

---

**작성일**: 2025-11-01
**버전**: 1.0.0
