# 바나나 중고컴퓨터 프로젝트 - 개발 가이드

> **⚠️ AI 세션 시작 시 이 파일을 가장 먼저 읽으세요!**
>
> 이 파일은 프로젝트의 현재 상태, 해결된 문제, 개발 규칙을 담고 있습니다.

---

## 📋 프로젝트 개요

### 프로젝트명
**바나나 중고컴퓨터 - 도매 매칭 플랫폼**

### 핵심 비즈니스 모델
- 중고 컴퓨터 **역경매 시스템**
- 일반 사용자가 매물을 올리면 → 도매상들이 경쟁적으로 매입가 제시
- 블라인드 입찰 (도매상들은 서로의 제안을 볼 수 없음)

### 기술 스택
- **Framework**: Next.js 14 (App Router)
- **Language**: TypeScript
- **Database**: Supabase (PostgreSQL)
- **Auth**: Supabase Auth (이메일 확인 필수)
- **State**: Zustand
- **Styling**: Tailwind CSS

---

## 🚨 중요: 이 프로젝트의 특별한 점

### ⚠️ Supabase SDK 사용 금지!

**이 프로젝트는 Supabase SDK 대신 Fetch API를 직접 사용합니다.**

**이유**:
- Supabase SDK 메서드들이 무한 대기(Hanging) 문제 발생
- `supabase.from().select()`, `supabase.auth.getSession()` 등이 멈춤
- RLS 정책과 SDK 충돌 문제

**해결책**:
- Fetch API로 완전 전환
- localStorage 기반 세션 관리
- 공통 유틸리티 함수 사용 (fetch.ts, logger.ts, storage.ts)

---

## ✅ 해결된 주요 문제 (2025-11-02)

### 문제 1: 에러 무한루프
**증상**: 에러 수정 후 기존 정상 작동 기능이 다시 에러 발생

**원인**:
1. localStorage 세션 관리 불일치
2. Fetch API 헤더 누락 (apikey, Authorization 등)
3. 에러 추적 어려움

**해결**:
- ✅ `lib/utils/fetch.ts` 생성 → Fetch API 통합 관리
- ✅ `lib/utils/logger.ts` 생성 → 체계적 로깅
- ✅ `lib/utils/storage.ts` 생성 → localStorage 세션 관리

**적용된 파일**:
- ✅ `lib/services/auth.service.ts` 리팩토링 완료
- ✅ `lib/services/product.service.ts` 리팩토링 완료

**아직 안 된 파일** (향후 작업 필요):
- ⏳ `lib/services/sell-request.service.ts`
- ⏳ `lib/services/storage.service.ts`
- ⏳ `lib/services/admin.service.ts`

---

## 📚 핵심 문서

### 반드시 읽어야 할 문서
1. **에러_무한루프_해결_가이드.txt** ⭐⭐⭐
   - 에러 발생 시 표준 작업 절차 (SOP)
   - 긴급 복구 매뉴얼
   - 90%의 에러 원인 3가지

2. **핵심개선_완료_리포트.txt** ⭐⭐
   - 생성된 유틸리티 상세 설명
   - 사용 방법 및 예제
   - Before/After 비교

3. **SUPABASE_SDK_CONVERSION_GUIDE.txt**
   - Supabase SDK → Fetch API 전환 가이드
   - Hanging 문제 해결 히스토리

4. **프로젝트_문서화_패키지/**
   - 데이터 모델 TypeScript 인터페이스
   - 화면별 상세 기능 명세서
   - 프로젝트 구조 제안서

---

## 🔥 절대 지켜야 할 개발 규칙

### 규칙 1: Fetch API 직접 사용 금지
```typescript
// ❌ 절대 금지!
const response = await fetch(url, { headers: { ... } });

// ✅ 반드시 이렇게!
import { get, post, patch, del } from '@/lib/utils/fetch';
const data = await get<Product[]>('/rest/v1/products');
```

### 규칙 2: localStorage 직접 접근 금지
```typescript
// ❌ 절대 금지!
const session = localStorage.getItem('sb-...');

// ✅ 반드시 이렇게!
import { getSession, getAccessToken } from '@/lib/utils/storage';
const session = getSession();
```

### 규칙 3: console.log 사용 금지
```typescript
// ❌ 절대 금지!
console.log('[Service] 작업 시작');

// ✅ 반드시 이렇게!
import { logger } from '@/lib/utils/logger';
logger.info('작업 시작', { data });
```

### 규칙 4: 작은 단위로 커밋
```bash
# ❌ 나쁜 예
git commit -m "상품 기능 전체 구현" (10개 파일 수정)

# ✅ 좋은 예
git commit -m "ProductService.createProduct() 구현" (1개 파일)
```

### 규칙 5: 30분 룰
- 30분 이상 막히면 즉시 롤백
- 더 작은 단위로 다시 시작

---

## 🛠️ 공통 유틸리티 사용법

### 1. fetch.ts - API 호출

```typescript
import { get, post, patch, del, getCount, fetchWithAuth } from '@/lib/utils/fetch';

// GET 요청
const products = await get<Product[]>('/rest/v1/products?select=*');

// POST 요청 (인증 필요)
const newProduct = await post<Product>('/rest/v1/products', {
  requireAuth: true,
  body: JSON.stringify({ title: '새 상품', price: 10000 })
});

// PATCH 요청
await patch('/rest/v1/products?id=eq.abc123', {
  requireAuth: true,
  body: JSON.stringify({ title: '수정된 제목' })
});

// DELETE 요청
await del('/rest/v1/products?id=eq.abc123', {
  requireAuth: true
});

// COUNT 조회 (개수만)
const count = await getCount('/rest/v1/products?seller_id=eq.abc123');

// 커스텀 요청 (필요시)
const response = await fetchWithAuth('/rest/v1/custom', {
  method: 'GET',
  requireAuth: true
});
```

**핵심 기능**:
- ✅ apikey, Authorization, Content-Type 자동 추가
- ✅ localStorage에서 accessToken 자동 획득
- ✅ 401 에러 시 자동 로그아웃 + 로그인 페이지 리다이렉트
- ✅ 403/500 에러 자동 경고
- ✅ API 호출 로깅 자동화

### 2. logger.ts - 로깅

```typescript
import { logger } from '@/lib/utils/logger';

// 기본 로그
logger.info('작업 시작', { userId: 'abc123' });
logger.warn('경고 메시지', { data: '...' });
logger.error('에러 발생', { error });

// 그룹 로그 (추천)
logger.group('AuthService.signIn');
logger.info('Step 1: 인증 확인');
logger.info('Step 2: 사용자 정보 조회');
logger.groupEnd();

// 성능 측정
const timer = logger.startTimer('상품 조회');
await getProducts();
timer.end(); // "상품 조회 completed in 123ms"

// API 호출 로그 (fetchWithAuth가 자동 호출)
logger.api('POST', '/rest/v1/products', 201, { duration: '123ms' });

// 테이블 로그
logger.table([
  { id: 1, name: '상품A', price: 1000 },
  { id: 2, name: '상품B', price: 2000 }
]);
```

**핵심 기능**:
- ✅ 개발 환경에서만 상세 로그 출력
- ✅ 프로덕션에서 에러는 Sentry로 전송 준비
- ✅ 타임스탬프 자동 추가
- ✅ API 호출 성공/실패 시각적 표시

### 3. storage.ts - localStorage 세션 관리

```typescript
import {
  getSession,
  getAccessToken,
  getUserId,
  setSession,
  clearSession,
  clearAllStorage
} from '@/lib/utils/storage';

// 현재 세션 확인 (자동 검증 포함)
const session = getSession();
if (session) {
  console.log('로그인 중:', session.user.email);
}

// Access Token만 필요
const token = getAccessToken();

// 사용자 ID만 필요
const userId = getUserId();

// 세션 저장 (로그인 시)
setSession(authData);

// 세션 삭제 (로그아웃 시)
clearSession();

// 전체 초기화 (개발 중 문제 발생 시)
clearAllStorage();
```

**핵심 기능**:
- ✅ localStorage 키 자동 계산
- ✅ 세션 데이터 검증 (access_token, user.id 필수)
- ✅ 만료된 세션 자동 감지 및 삭제
- ✅ SSR 안전성 확보

---

## 🐛 에러 발생 시 대응

### 90% 에러의 3대 원인

1. **localStorage 세션 문제**
   - 증상: 로그인했는데 바로 로그아웃됨, getCurrentUser() 실패
   - 해결: `localStorage.clear()` + 페이지 새로고침

2. **Fetch API 헤더 누락**
   - 증상: 401 Unauthorized, 403 Forbidden
   - 해결: `fetchWithAuth()` 사용 확인, `requireAuth: true` 추가

3. **RLS 정책 충돌**
   - 증상: 403 Forbidden, 데이터 조회 안 됨
   - 해결: Supabase Dashboard → SQL Editor → RLS 정책 확인

### 표준 작업 절차 (SOP)

**Step 1**: 에러 정보 수집 (5분)
- 브라우저 콘솔 에러 메시지
- 네트워크 탭 (실패한 API 요청)
- localStorage 세션 확인

**Step 2**: 에러 원인 분류 (3분)
- 컴파일 에러 (TypeScript)
- 런타임 에러 (JavaScript)
- API 에러 (Fetch)
- UI 렌더링 에러 (React)

**Step 3**: 수정 (20분)
- 최소한의 변경만 적용
- 즉시 테스트

**Step 4**: 30분 초과 시 → 즉시 롤백
```bash
git stash save "작업 중단"
git reset --hard HEAD~1
```

### 긴급 복구

**상황 1: 앱 완전 작동 불가**
```bash
git reset --hard [정상_커밋_해시]
localStorage.clear()
```

**상황 2: 인증 완전 깨짐**
- Supabase Dashboard → Authentication → Users → 테스트 계정 삭제
- `DELETE FROM public.users;`
- `localStorage.clear()`

**상황 3: RLS 정책 문제**
```sql
-- 긴급: 모든 RLS 비활성화
ALTER TABLE users DISABLE ROW LEVEL SECURITY;
ALTER TABLE products DISABLE ROW LEVEL SECURITY;
-- 테스트 후 하나씩 재활성화
```

---

## 📊 프로젝트 현재 상태

### 완료된 기능 (40%)
- ✅ 인증 시스템 (회원가입, 로그인, 이메일 확인)
- ✅ 사업자 인증 시스템
- ✅ 관리자 대시보드
- ✅ 상품 목록 조회
- ✅ 핵심 유틸리티 (fetch, logger, storage)

### 진행 중
- 🔄 상품 등록/수정 UI
- 🔄 상품 상세 페이지

### 대기
- ⏳ 매입 요청/제안 UI (서비스 레이어는 완료)
- ⏳ 문의 시스템
- ⏳ 도매상 대시보드

---

## 🎯 다음 작업 우선순위

### Phase 1: 나머지 서비스 파일 리팩토링
1. `sell-request.service.ts` → fetchWithAuth() 사용
2. `storage.service.ts` → logger 사용
3. `admin.service.ts` → fetchWithAuth() + logger 사용

### Phase 2: 상품 기능 완성
1. 상품 등록 UI 구현
2. 상품 상세 페이지 구현
3. 상품 수정/삭제 기능

### Phase 3: 역경매 시스템
1. 매입 요청 등록 UI
2. 매입 제안 제출 UI
3. 거래 확정 플로우

### Phase 4: 품질 향상
1. Sentry 연동 (에러 모니터링)
2. Jest 테스트 자동화
3. Supabase RPC 함수 (트랜잭션 처리)

---

## 📂 프로젝트 구조

```
E:\Project_doing\banana_usedcomputer_web\
├── app/                          # Next.js App Router
│   ├── (auth)/                   # 인증 라우트
│   ├── (main)/                   # 보호된 라우트
│   └── api/                      # API Routes
│
├── lib/                          # 핵심 로직
│   ├── utils/                    # 🆕 공통 유틸리티
│   │   ├── fetch.ts             # ⭐ Fetch API 통합
│   │   ├── logger.ts            # ⭐ 로깅 시스템
│   │   └── storage.ts           # ⭐ localStorage 관리
│   ├── services/                # 비즈니스 로직
│   │   ├── auth.service.ts      # ✅ 리팩토링 완료
│   │   ├── product.service.ts   # ✅ 리팩토링 완료
│   │   ├── sell-request.service.ts  # ⏳ 대기
│   │   ├── storage.service.ts   # ⏳ 대기
│   │   └── admin.service.ts     # ⏳ 대기
│   ├── hooks/
│   └── store/
│
├── components/                   # React 컴포넌트
├── types/                        # TypeScript 타입
├── constants/                    # 상수
│
├── 에러_무한루프_해결_가이드.txt     # ⭐⭐⭐ 필독
├── 핵심개선_완료_리포트.txt          # ⭐⭐ 필독
├── SUPABASE_SDK_CONVERSION_GUIDE.txt
├── supabase-setup.sql            # DB 초기 설정
└── .env.local                    # 환경 변수
```

---

## 🔑 환경 변수 (.env.local)

```bash
NEXT_PUBLIC_SUPABASE_URL=https://vqypnenjejbtvvvewxee.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGc...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGc...
NEXT_PUBLIC_APP_NAME=바나나 중고컴퓨터
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

---

## 💡 개발 팁

### localStorage 정기 초기화
```javascript
// 브라우저 콘솔에서
localStorage.clear()
location.reload()
```

**권장 시점**:
- 매일 개발 시작 전
- auth.service.ts 수정 후
- 세션 관련 에러 발생 시

### 빠른 디버깅
```typescript
// logger 그룹으로 디버깅
logger.group('디버깅: 상품 등록');
logger.info('입력 데이터', productData);
logger.info('API 응답', response);
logger.groupEnd();
```

### 타입 에러 해결
```bash
# TypeScript 컴파일 확인
npm run build

# ESLint 확인
npm run lint
```

---

## 🎊 마지막 체크리스트

새로운 기능 개발 전 확인:

- [ ] `에러_무한루프_해결_가이드.txt` 읽었는가?
- [ ] `fetchWithAuth()` 사용할 준비 되었는가?
- [ ] `logger` import 했는가?
- [ ] localStorage 직접 접근 안 하는가?
- [ ] 작은 단위로 커밋할 계획인가?
- [ ] 30분 룰 기억하는가?

---

## 📞 문제 발생 시

1. 브라우저 콘솔 확인
2. 네트워크 탭 확인
3. localStorage 확인
4. **에러_무한루프_해결_가이드.txt** 참고
5. 30분 이상 막히면 롤백

**에러가 발생하면 당황하지 말고, 가이드를 따르세요!** 🚀

---

**마지막 업데이트**: 2025-11-02
**프로젝트 진행률**: 40%
**다음 마일스톤**: 상품 기능 완성 (목표: 60%)
